import { defineMessages } from 'react-intl';

export const shape = {
  header: {
    id: 'mp.containers.NotFoundPage.header',
    defaultMessage: 'Page not found.',
  },
};

export default defineMessages(shape);
